"""adp.mcp — Model Context Protocol client integration for ADP.

Sub-modules:
    config    — MCPServerConfig dataclass + load_mcp_config()
    registry  — MCPTool + ToolRegistry
    client    — MCPClientManager (async context manager)
    resolver  — resolve_tool_args() for building tool call arguments
"""
